services:
  - type: web
    name: tetris-battle
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node server/src/index.js
    envVars:
      - key: NODE_ENV
        value: production
